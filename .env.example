FLASK_APP=wsgi.py
FLASK_CONFIG=development
SECRET_KEY=change-me-in-production

# Optional: how long (in seconds) shared files and clipboard text stick
# around before being auto-deleted, and how often the server checks.
# FILE_EXPIRY_SECONDS=300
# CLIPBOARD_EXPIRY_SECONDS=300
# CLEANUP_INTERVAL_SECONDS=30
